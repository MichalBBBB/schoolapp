mutation CreateTask(
  $name: String!
  $subjectId: String
  $dueDate: DateTime
  $doDate: DateTime
  $id: String!
) {
  createTask(
    id: $id
    name: $name
    subjectId: $subjectId
    dueDate: $dueDate
    doDate: $doDate
  ) {
    ...Task
  }
}
