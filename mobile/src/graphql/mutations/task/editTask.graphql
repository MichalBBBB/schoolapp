mutation EditTask(
  $id: String!
  $name: String!
  $text: String
  $dueDate: DateTime
  $doDate: DateTime
  $reminders: [RemindersInput!]
) {
  editTask(
    id: $id
    name: $name
    text: $text
    dueDate: $dueDate
    doDate: $doDate
    reminders: $reminders
  ) {
    ...Task
  }
}
