mutation CreateSubject(
  $name: String!
  $id: String!
  $colorName: String!
  $extraInfo: String
) {
  createSubject(
    name: $name
    id: $id
    colorName: $colorName
    extraInfo: $extraInfo
  ) {
    ...Subject
  }
}
