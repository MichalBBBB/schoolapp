mutation CreateLessonTime(
  $endTime: String!
  $startTime: String!
  $id: String!
  $scheduleId: String!
) {
  createLessonTime(
    endTime: $endTime
    startTime: $startTime
    id: $id
    scheduleId: $scheduleId
  ) {
    ...LessonTime
  }
}
