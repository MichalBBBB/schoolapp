mutation UserExists($email: String!) {
  userExists(email: $email) {
    ... on UserExistsSucces {
      userExists
    }
    ... on UserFail {
      errors {
        field
        message
      }
    }
  }
}
