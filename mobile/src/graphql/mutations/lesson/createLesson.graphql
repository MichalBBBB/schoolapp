mutation CreateLesson(
  $dayNumber: Float!
  $lessonTimeId: String!
  $subjectId: String!
  $id: String!
) {
  createLesson(
    id: $id
    dayNumber: $dayNumber
    lessonTimeId: $lessonTimeId
    subjectId: $subjectId
  ) {
    ...Lesson
  }
}
