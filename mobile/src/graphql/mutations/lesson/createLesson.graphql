mutation CreateLesson(
  $dayOfTheWeek: String!
  $lessonTimeId: String!
  $subjectId: String!
  $id: String!
) {
  createLesson(
    id: $id
    dayOfTheWeek: $dayOfTheWeek
    lessonTimeId: $lessonTimeId
    subjectId: $subjectId
  ) {
    ...Lesson
  }
}
