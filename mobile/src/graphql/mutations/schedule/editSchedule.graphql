mutation EditSchedule(
  $id: String!
  $name: String
  $dayNumbers: [Float!]
  $dates: [DateTime!]
) {
  editSchedule(id: $id, name: $name, dayNumbers: $dayNumbers, dates: $dates) {
    ...Schedule
  }
}
