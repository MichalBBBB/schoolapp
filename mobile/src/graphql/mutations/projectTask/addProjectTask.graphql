mutation AddProjectTask(
  $id: String!
  $name: String!
  $dueDate: DateTime
  $doDate: DateTime
  $projectId: String!
) {
  addProjectTask(
    name: $name
    dueDate: $dueDate
    projectId: $projectId
    doDate: $doDate
    id: $id
  ) {
    ...ProjectTask
  }
}
