mutation AddProjectTask(
  $name: String!
  $dueDate: DateTime
  $doDate: DateTime
  $projectId: String!
) {
  addProjectTask(
    name: $name
    dueDate: $dueDate
    projectId: $projectId
    doDate: $doDate
  ) {
    ...ProjectTask
  }
}
